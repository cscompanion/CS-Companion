<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itp.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YagovkinInfo" />
        <activity android:name=".SergeyevnaInfo" />
        <activity android:name=".CharpentierInfo" />
        <activity android:name=".TraversInfo" />
        <activity android:name=".JuricInfo" />
        <activity android:name=".DupuisInfo" />
        <activity android:name=".ClientsPage" />
        <activity android:name=".FirstPage" />
        <activity android:name=".EmailPasswordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>